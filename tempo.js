let token = {
    octal: true,
    // other properties
};

if (token.octal) {
    // Execute this block if token.octal is truthy
    console.log('Token is octal.');
} else {
    // Execute this block if token.octal is falsy
    console.log('Token is not octal.');
}
